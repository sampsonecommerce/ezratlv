# INERT FILE — confirmed 2026-07-15 (curl: x-github-request-id present, /iruvei-hevra returns 404
# instead of redirecting). The live origin is GitHub Pages, not Cloudflare Pages, so this file is
# never read; `_redirects` only works when Cloudflare Pages (or Netlify, via netlify.toml) is the
# HOST. Cloudflare here is DNS/CDN-proxy only (orange-cloud), sitting in front of GitHub Pages.
# The real mechanism: Cloudflare dashboard → that domain's zone → Rules → Redirect Rules (or Bulk
# Redirects for many at once) — these run at Cloudflare's edge before the request ever reaches
# GitHub Pages, so they work regardless of origin. Every rule below still needs to be recreated
# there by hand; this file is kept only as a record of intended redirects.
# /api/submit-lead is handled automatically by functions/api/submit-lead.js, so it's not here.

/iruvei-hevra        /company-events   301
/iruvei-hevra.html   /company-events   301

# transient thank-you URL (set client-side via pushState); a direct hit/refresh returns to the booking page
/company-events/confirmation           /company-events           302
/english-company-events/confirmation   /english-company-events   302
/he                  /                 301
/he/*                /                 301

# recovered SEO from the old Wix event pages (rescue their link equity into the new site)
/he/event-list                                /   301
/he/event-details/hananvedan                  /   301
/event-details/ezra-jazz-hang-haynes-tribute  /   301
/event-details/stndp-bzr                      /   301
/event-details/*                              /   301
/he/event-details/*                           /   301

/*                   /                 301
